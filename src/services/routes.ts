/* eslint-disable no-unused-vars */
//Authentication
export enum AUTH_ROUTES {
  LOGIN = '/accounts/login/',
}

export enum ACCOUNTREGISTER_ROUTES {
  SIGNUP = '/accounts/register/',
}

export enum USERSPROFILE_ROUTES {
  USERS = '/users/profile'
}