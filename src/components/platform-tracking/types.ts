export interface iCourse {
    sn: number | string;
    name: string;
    modules: number;
    activation: number | string;
    completion: number | string;
    views: number | string;
    status: string;
  }